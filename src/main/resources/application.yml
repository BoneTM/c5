mybatis:
  mapper-locations: classpath:mapper/*.xml
server:
  port: 8888
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    password: 123890
    url: jdbc:mysql://182.92.161.193:3306/news?useUnicode=true&characterEncoding=utf-8
    username: root

  redis:
    host: locahost
    port: 6379
    password:
    database: 0
    jedis:
      pool:
        #最大连接数
        max-active: 8
        #最大空闲
        max-idle: 8
        #最大阻塞等待时间(负数表示没限制)
        max-wait: -1ms
        #最小空闲
        min-idle: 0
    #连接超时时间
    timeout: 1000ms

# JWT 配置
jwt:
  header: Authorization
  secret: Bone
  expiration: 36000